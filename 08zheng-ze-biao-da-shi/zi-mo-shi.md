#### 正则表达式中的 子模式

```php
<?php

    // 正则表达式
    $ptn = '/<img src=.*?\/>/';    // 匹配整个标签
    $ptn = '/<img src=(.*?)\/>/';  // 匹配整个标签, 小括号中的内容会生成一个子数组, 称为子模式

    // 目标字符串
    $str = "<img src='./a/b.jpg' />";
    
    // 执行匹配函数
    preg_match_all($ptn, $str, $arr);
    
?>
```





