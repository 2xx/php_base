#### 用 for 循环遍历数组

```php
<?php

    $arr = ['a', 'b', 'c', 'd'];

    $n = count( $arr );       // 统计数组元素个数

    for($i = 0; $i < $n; $i++){
        echo $arr[ $i ];
    }
​
    /*
        数组的下标, 必须是连续的索引数组
    */
    
?>
```



