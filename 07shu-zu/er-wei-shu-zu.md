#### 二维数组

数组中的元素, 还是数组

```php
<?php

    $a = ['a', 'b', 'c'];
    $b = ['d', 'e', 'f'];
    $c = ['g', 'h', 'i'];
​
    $arr = [ $a, $b, $c ];  // 二维数组

    // 一般是下面这样的写法, 不需要$a,$b,$c这三个变量名
    $arr = [
        ['a', 'b', 'c'],
        ['d', 'e', 'f'],
        ['g', 'h', 'i']
    ];

?>
```



